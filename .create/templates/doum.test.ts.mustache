import { describe, expect, test } from 'vitest'
import { {{functionName}} } from './{{functionName}}';

describe("{{type}}|{{functionName}}", () => {  
  test('', () => {
    expect({{functionName}}(0)).toBeTruthy(); 
  });
});